Ticket: TKT-1042
Environment: production
Service: identity-api

Since this morning, users who leave the application idle for around 30 minutes are receiving HTTP 401 when the client attempts to refresh the session.
Support has confirmed 37 affected users.

The issue is functional: users must sign in again to continue working.

One engineer wrote "maybe the new cache is keeping stale token metadata", but no root cause has been confirmed.

No credentials or tokens are included in this ticket.
