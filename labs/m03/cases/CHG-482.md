# CHG-482 — Token refresh cache update

```yaml
change_id: CHG-482
service: account-api
component: token-refresh-cache
change_type: application
production_target: true

summary:
  - "Reduce repeated token refresh lookups by caching token lifecycle metadata."
  - "Changes refresh-token handling after idle sessions."

security_scope:
  authentication: true
  authorization: false
  secrets: false
  token_lifecycle: true

tests:
  status: executed
  detailed_results: external_test_system

rollback:
  plan: documented
  rehearsal_evidence: external_change_system

monitoring:
  dashboard: referenced
  detailed_status: external_observability_system

security_review:
  review_id: SEC-884
  status: external_security_system

known_issues:
  status: external_issue_tracker

owner: platform-identity
```

Las referencias a sistemas externos no equivalen a PASS.
